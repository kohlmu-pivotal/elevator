package com.elevator.state.core

class ArbitraryRunner(val elevator: Elevator) {

    fun runElevators() {
//        val instances = (0..500).map { index ->
//            elevator.createNewInstance("E$index")
//        }.toList()
//
//        instances.forEach { instance ->
//            val stateProcessContext = elevator.processEvent(ElevatorEvents.TURN_ON, instance)
//            elevator.processEvent(ElevatorEvents.FLOOR_SELECTED, stateProcessContext)
//        }
    }
}